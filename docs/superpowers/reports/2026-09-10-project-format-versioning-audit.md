# Audit du chantier — versionnement de format de projet et convertisseurs

Date: 2026-09-10
Status: **Chantier C clos, corrigé par trois tours de revue de branche.** Les 8 tâches sont satisfaites. Le plan `2026-09-08-project-format-versioning-and-converters.md` livre le stamp de génération, le refus vers l'arrière, le pipeline de conversion avec consentement et sauvegarde, et le retrait du rôle d'autorisation de contenu de `ManifestVersion`. **Il ne livre pas la suppression du reniflage de forme qui le motivait** : cette promesse est re-cadrée et consignée en écart 33. Une revue de branche postérieure à la clôture a trouvé et corrigé trois défauts Critical et trois Important dans le chemin de conversion réel (rulings 34-39, §8); un second tour, vérifiant ces corrections, a trouvé que deux d'entre elles avaient régressé C2 et la spec (rulings 40-43, §9); un troisième tour a trouvé que le déplacement du tour 2 n'allait pas assez loin, qu'une ligne de couverture affirmait une propriété non gardée, et qu'un test de fermeture de revue ne discriminait pas (rulings 44-47, §10). Écart 35 (§6) : trois modules sur quatre n'ont aucun refus vers l'arrière.
Document version: `V2.1.6.0023`

## Historique des changements

| Date | Version | Commit | Changement |
| --- | --- | --- | --- |
| 2026-09-10 | `V2.1.6.0023` | `PENDING` | Revue de branche, tour 3 (rulings 44-47, §10) : le recouvrement de transaction, déplacé au tour 2 dans la branche de conversion mais encore au-dessus de la porte `CanProceed`, touchait donc le disque avant même que l'opérateur ait répondu au dialogue — déplacé sous `CanProceed`, juste avant l'écriture (Ruling 44, Critical); la ligne de couverture du tour 2 créditait `BackwardRefusalTests` d'une propriété non assertée — la même assertion de répertoire que le test trop-récent est ajoutée au test de conversion refusée (Ruling 46); le test du reste de C3 ne discriminait pas le convertisseur du normaliseur en mémoire — assertion déplacée sur le fichier `project.json` lu depuis le disque (Ruling 47); §8 annoté (pas réécrit) pour signaler son placement dépassé, dernière citation vers `branch-fix-report.md` retirée de la ligne de changelog `V2.1.6.0021` de la spec. La remontée de garde proposée par le relecteur dans `ModernProjectStore` est délibérément non faite, consignée en amélioration nommée (Ruling 45). |
| 2026-09-10 | `V2.1.6.0022` | `240dcf6` | Revue de branche, tour 2 (rulings 40-43, §9) : le recouvrement de transaction ajouté au tour 1 créait `.studio/`+le verrou dans un projet sur le point d'être refusé comme trop récent (N1/Ruling 40, Critical) — déplacé à l'intérieur de la branche de conversion; spec §5.2/§5.4 périmées corrigées plutôt que la seule ligne Status (Ruling 42); §8 corrigé (comptage exact, preuve empirique rapatriée, citations vers un chemin git-ignoré retirées — Ruling 43); écart 35 ajouté à `KNOWN_GAPS_V2.md` (trois modules sur quatre sans refus). Inclus dans la même passe : test bout-en-bout du reste de C3 (règlement de `PageKey` sur disque), BOM UTF-8 retiré de l'écriture de conversion, branche morte retirée du descripteur d'erreur du convertisseur, nettoyage du fichier temporaire isolé de l'exception réelle. |
| 2026-09-10 | `V2.1.6.0021` | `c1e5b6c` | Corrections de revue de branche, tour 1 (rulings 34-39, §8) : la récupération de transaction s'exécute désormais avant la pré-lecture de génération dans `OpenAsync` (Ruling 34, Critical — une reprise pouvait annuler silencieusement une conversion consentie; déplacée au tour 2, voir ci-dessus) ; l'écriture de conversion est atomique par fichier temporaire + renommage (Ruling 35, Critical) ; le convertisseur `Generation1` reprend exactement `EffectivePageCode` au lieu de `PageCode ?? Id ?? ""` (Ruling 37, Important) ; la lecture JSON de la conversion est insensible à la casse et utilise `ArtifactFormatVersionReader.FieldName` (Ruling 38, Important) ; le chemin de sauvegarde retourné par `ArtifactBackupWriter.CreateBackup` est désormais rapporté dans le diagnostic `project.open-failed` (Ruling 39, Important). Ajout du premier test bout-en-bout du chantier convertissant réellement un projet génération 0 sur disque (Ruling 36, Critical). |
| 2026-09-10 | `V2.1.6.0020` | `d3be7da` | Clôture du chantier de versionnement de format : rapport d'audit, sort de chaque règle de `ModernProjectMigration`, état des générations par module. |

## 1. Portée et conclusion

Le chantier existe parce qu'aucun fichier de projet ne portait sa version de format. Trois champs de version existaient — `ScadaVersion`, `ManifestVersion`, `SchemaVersion` du `.sep` — et **aucun n'était le format du projet**. La conséquence était mesurable et vivante : `ScadaProject.QuickWindows` et `QuickWindowInvocations`, propriétés nullables ajoutées en Phase 1 de `DEC-0050`, disparaissaient silencieusement à la première sauvegarde d'un binaire antérieur, avec toutes les définitions, interfaces locales et invocations. `DEC-0049` D5 spécifiait le refus; le dépôt ne refusait que `project.open-path-invalid` et `project.open-failed`.

**Ce défaut est fermé.** Un projet déclarant une génération supérieure à celle du binaire est refusé au lieu d'être réécrit.

**25 commits**, `V2.1.6.0011` → `V2.1.6.0020`, suite de **912 à 968 tests sans skip**, `verify-docs` à `Errors: 0` et 121 avertissements du début à la fin, **zéro dérive de fixture gelée** (`git diff 3301329..HEAD -- '*.sep' '*.sb2' '*.sha256'` est vide).

## 2. Générations par module

| Module | Génération | Porteur | Convertisseur |
| --- | --- | --- | --- |
| `project.json` | 1 | `FormatVersion` (nouveau) | `ProjectGeneration1Converter` (0 → 1) |
| Scène | 0 | `FormatVersion` (nouveau) | aucun |
| Catalogue de tags | 0 | `FormatVersion` (nouveau) | aucun |
| Composant `.sep` | 1 | `SchemaVersion` (existant, raccordé) | aucun |

Le `.sep` n'a pas reçu de champ : il en portait déjà un, jamais consulté comme génération. Le raccorder valait mieux que d'en ajouter un second.

Trace de bookkeeping, consignée ici parce qu'elle n'existe nulle part ailleurs : le commit **`902418b`**, qui raccorde le module `.sep` à son `SchemaVersion`, **ne porte ni bump de `VERSION` ni ligne de changelog**. Ma dépêche en exigeait un, le brief n'en prévoyait pas, et l'implémenteur a suivi le brief — choix jugé correct à la revue. Plutôt que de renvoyer la tâche pour une correction purement comptable, la trace documentaire de ce commit est portée ici (Ruling 12).

## 3. Sort de chaque règle de `ModernProjectMigration`

C'était le point de la Tâche 7 : chaque règle reprise devait être **retirée** dans le même commit. **Une seule l'a été partiellement, et cinq sont conservées.** Le tableau dit pourquoi, règle par règle.

| Règle | Sort | Raison |
| --- | --- | --- |
| Back-fill de `PageKey` sur une page sans clé | **Reprise dans le fichier**, conservée en mémoire | Le convertisseur la fixe une fois sur le disque; `MigrateProject` la garde pour les projets construits en mémoire qui ne viennent d'aucun fichier |
| Canonicalisation `Id`/`PageCode` | Conservée | Invariant maintenu à chaque sauvegarde, pas une question d'âge de fichier |
| Dérivation de `Origin` | Conservée | Idem |
| Synthèse et fusion d'`ImportProvenance` | Conservée | Dépend d'un **inventaire d'import en mémoire** qu'un convertisseur de fichier ne possède pas |
| Résolution `HeaderPageKey`/`FooterPageKey` | Conservée | Idem, recalculée après chaque édition de composition |
| Résolution `HomePageKey`/`HomePageId` | Conservée | Idem |

**La prémisse du plan était fausse.** `MigrateProject` n'est pas une migration au chargement : c'est le normaliseur d'identité du store, appelé depuis **11 sites**, dont deux écritures (`SaveProjectToRootAsync`) et une construction en mémoire sans fichier (`EnsureReferenceProjectAsync`, qui migre un `ScadaProject` bâti depuis un inventaire d'import Wonderware). Un convertisseur s'exécute une fois, sur un fichier, à l'ouverture; il ne peut pas tenir un invariant qui doit valoir après chaque édition en mémoire et avant chaque écriture.

La suppression forcée a été tentée et a rendu **7 tests rouges** — `ModernProjectStoreTests` ×3, `PageIdentityTests` ×3, `PageDependencyAnalyzerTests` ×1. La couverture existante a arrêté la régression. Le fichier a été restauré et la promesse re-cadrée plutôt que forcée.

Retirer `ModernProjectMigration` reste possible, mais exige d'abord de déplacer l'établissement d'identité de page à l'import vers l'importeur lui-même. C'est un chantier distinct, non commencé, consigné en écart 33.

## 4. Ce que le chantier corrige au-delà de son objet

**Un blocage latent total sur les Fenêtres rapides.** `ValidateQuickWindows` comparait `project.ManifestVersion` à `"2.3"` pour autoriser le contenu. Or ce champ **persisté** vaut `"2.0"` par défaut, le projet de référence porte `"2.0"`, et **aucune ligne de `src/` ne lui assigne jamais `"2.3"` ** — cette chaîne n'est écrite que dans le manifeste **exporté**, un artefact différent. Le contrôle aurait donc levé `quick-window.profile-unsupported` en **Error** sur tout projet réel dès qu'il aurait porté une Fenêtre rapide. Il n'a jamais sauté seulement parce qu'aucun projet réel n'en porte encore.

La Tâche 8 retire ce contrôle. Ce n'est pas une réduction de portée d'une garde en état de marche : c'est la correction d'une mine sous la fonctionnalité que `DEC-0050` a livrée. L'autorisation de contenu revient au catalogue de capacités, qui porte le statut, la preuve à trois couches et le gate fail-closed qu'une comparaison de chaîne ne pouvait pas porter.

## 5. Défauts trouvés et corrigés en cours de chantier

- **`ResolveChain` sur-convertissait.** Un convertisseur 0 → 3 interrogé pour 0 → 2 rendait une chaîne marquée complète qui dépassait la cible. Non atteignable par l'appelant actuel, mais une conversion ne se défait pas : sur-convertir est irréversible. Le dépassement est désormais traité comme une chaîne incomplète — refuser vaut mieux que convertir trop loin.
- **Le convertisseur inventait une seconde dérivation d'identité.** Un SHA-256 brut du seul code de page, sans le nom du projet, sans repli de casse et sans les bits de version et de variante — là où `PageKeyFactory.CreateDeterministic` produit un GUID v5 sur `nom|code`. Deux pages sans code auraient reçu la **même** clé. Aligné, puis épinglé par un littéral (`e6ce2175-1047-5dbc-ac17-fdaa0840b615` pour `("P","win00001")`) recalculé indépendamment.
- **Une `ArgumentException` s'échappait jusqu'à l'UI.** Le `catch` d'`OpenAsync` est filtré sur cinq types; `CreateDeterministic` lève `ArgumentException`, absente du filtre. Un nom de projet ou un code de page vide traversait `Apply`, `OpenAsync` et le coordinateur — qui n'a qu'un `finally` — sans produire aucun diagnostic. Le convertisseur valide désormais lui-même et lève `InvalidDataException`, type déjà couvert.
- **La sauvegarde laissait des débris.** Écrite avant la conversion, elle survivait à une conversion qui échoue, et chaque nouvelle tentative en ajoutait une puisque le suffixe est numéroté sans réemploi. Déplacée juste avant l'écriture : convertir en mémoire ne modifie rien sur disque, donc C6 reste satisfait à la lettre.

## 6. Écarts ouverts

- **Écart 33** — la suppression du reniflage de forme n'est pas livrée. Voir §3.
- **Écart 34** — le contenu Fenêtre rapide n'est gardé sous aucun profil `Compatibility21`/`Compatibility22`. Le correctif est nommé : un contrôle structurel dans `Ft100PackageValidation.ValidateQuickWindows`, sur le modèle exact de celui de `RuntimeContract`. Non urgent : aucun chemin produit ne passe ces profils.
- **Écart 35 — trois modules sur quatre n'ont aucun refus vers l'arrière.** Seul `project.json` est effectivement gardé : `ProjectWorkspaceRepository.OpenAsync` est le seul appelant qui lit une génération déclarée et refuse. `ArtifactModule.Scene`, `.TagCatalog` et `.Component` n'ont aucune référence de production (vérifié : seuls des tests de `ArtifactConverterRegistryTests` les utilisent) — rien dans le produit ne construit jamais un `ArtifactToConvert` pour ces trois modules. `ScadaScene.FormatVersion` et `ScadaTagCatalog.FormatVersion` (`ScadaSceneModels.cs`, `ProjectModels.cs`) existent et sont sérialisés/désérialisés (aller-retour), mais **aucun code ne les lit pour décider quoi que ce soit** — ni refus, ni conversion. La spec §7 exige un refus par module; ce n'est pas un enrichissement futur, c'est un gate qui n'existe pas pour trois artefacts sur quatre aujourd'hui. `PROJECT_MODEL_CONTRACT_V2.md` §5 est corrigée dans la même passe : sa première phrase se lisait comme si les quatre modules portaient un gate en service, alors qu'un seul le fait. `KNOWN_GAPS_V2.md` entrée 35.
- **Préexistant, non imputable au chantier** — `blockedQuickWindowCapabilities` dans `ProjectModels.cs` omet `QuickWindowLegacyFragmentAdapter`, que la liste agrégée de l'analyseur porte. La capacité est refusée à l'export mais pas à la validation de build : plus tard et plus cher, jamais silencieusement.
- **Préexistant, non imputable au chantier** — `AcquireWorkspaceLockAsync` (`ModernProjectStore.cs:772-789`) attend indéfiniment un verrou de fichier disputé, en boucle sur `IOException`, échappable seulement par annulation du jeton; `MainWindow` appelle le chemin d'ouverture avec le jeton d'annulation par défaut, donc non annulable en pratique. Ce chemin existait avant ce chantier. Le tour 2 de la revue de branche (Ruling 41) retire seulement l'exposition *nouvelle* que ce chantier avait introduite sur le chemin de refus (voir §9) — l'attente infinie préexistante pour les ouvertures qui atteignent effectivement le store reste entière. `KNOWN_GAPS_V2.md` entrée 35.

## 7. Ce qui reste à valider humainement

Aucun test humain n'a été exécuté sur ce chantier. Le parcours qui compte est celui-ci : **ouvrir un projet antérieur, lire le plan de conversion, refuser, vérifier que rien n'a bougé; puis rouvrir, accepter, vérifier la sauvegarde et le projet converti.** Les tests couvrent chaque moitié; seul un opérateur peut dire si le dialogue explique ce qu'il fait à quelqu'un qui ne l'a pas écrit.

## 8. Corrections de revue de branche (rulings 34-39)

Une revue de branche sur `studio-element-plus-drawing`, postérieure à la clôture ci-dessus, a trouvé que **la suite verte à 968/968 ne prouvait pas le chemin qui compte** : aucun test n'ouvrait réellement un projet génération 0 valide et laissait la conversion s'exécuter jusqu'au bout. Deux défauts Critical vivaient sous ce trou de couverture.

- **Ruling 34 (Critical) — une reprise pouvait annuler une conversion consentie.** `ProjectWorkspaceRepository.OpenAsync` convertissait et écrivait `project.json` en place, *puis* lisait l'instantané via `ModernProjectStore.ReadWorkspaceSnapshotFromProjectRootAsync`, dont le premier geste est `RecoverIncompleteTransactionsAsync`. Une transaction de sauvegarde interrompue laissée dans `.studio/transactions/` était donc rejouée **après** la conversion, écrasant potentiellement le fichier fraîchement converti avec sa sauvegarde de transaction pré-conversion — annulant silencieusement ce que l'opérateur venait d'accepter. Corrigé en exposant `ModernProjectStore.RecoverPendingTransactionsAsync` (instance publique déléguant au recouvrement privé) et en l'appelant en tête d'`OpenAsync`, avant la pré-lecture de génération. La récupération étant idempotente, l'appeler une deuxième fois plus loin dans le chemin normal ne coûte rien. **Ce placement est dépassé** : le tour 2 (Ruling 40, §9) a déplacé l'appel dans la branche de conversion parce que ce placement recréait `.studio/`/le verrou dans un projet sur le point d'être refusé comme trop récent; le tour 3 (Ruling 44, §9) l'a déplacé une seconde fois, après la porte `CanProceed`, parce que l'opérateur pouvait encore refuser la conversion après que le verrou et la reprise aient déjà touché le disque. Emplacement actuel : voir §9.
- **Ruling 35 (Critical) — l'écriture de conversion n'était pas atomique.** `File.WriteAllTextAsync` écrivait directement sur le fichier vivant, alors que §2.1 de la spec justifie toute l'approche sur une écriture en zone tampon puis renommage. Corrigé : écriture sur un fichier temporaire adjacent (`FileOptions.WriteThrough`, flush explicite), puis `File.Move(..., overwrite: true)`; le fichier temporaire est nettoyé si l'écriture échoue avant le renommage.
- **Ruling 36 (Critical) — aucun test ne convertissait réellement un projet.** Le seul artefact génération 0 (`BackwardRefusalTests.cs`) utilisait un consentement refusé; tous les tests à consentement accepté écrivaient déjà la génération courante. Ajout de `tests/ScadaBuilderV2.Tests/Formats/ProjectConversionEndToEndTests.cs` : un projet valide (scènes réelles, créé par le chemin de création réel) est rétrogradé à la génération 0 puis ouvert avec consentement accepté — l'ouverture réussit, le fichier porte `FormatVersion: 1`, le `.bak` porte les octets d'origine, et une seconde ouverture ne redemande rien ni ne réécrit rien. Un second test place une transaction de sauvegarde en attente avant l'ouverture : il échoue de manière démontrée avant le correctif du Ruling 34 (le fichier retombe à la génération 0) et passe après.
- **Ruling 37 (Important) — le repli de code de page du convertisseur divergeait du domaine.** Le domaine : `EffectivePageCode => string.IsNullOrWhiteSpace(PageCode) ? Id : PageCode`. Le convertisseur : `PageCode ?? Id ?? ""`, qui ne se replie pas sur `Id` quand `PageCode` est présent mais blanc — une page qui s'ouvrait sans problème sur le commit de base devenait **définitivement inouvrable** une fois convertie, sans mode lecture seule pour la récupérer. Corrigé en reprenant exactement la même expression que `EffectivePageCode`. Le test qui épinglait le refus (`APageWithAWhitespacePageCodeFailsConversionWithInvalidDataException`) est remplacé par un test qui affirme le succès du repli sur `Id`; un test symétrique couvre toujours le cas réellement les-deux-blancs.
- **Ruling 38 (Important) — lectures sensibles à la casse contre la contrainte globale du plan.** `ArtifactFormatVersionReader` compare en `OrdinalIgnoreCase`; le convertisseur lisait via l'indexeur de `JsonObject`, sensible à la casse. Sur un manifeste portant `"formatversion"`, le lecteur le trouvait, le convertisseur non — il ajoutait donc une seconde propriété `"FormatVersion"` correctement casée, et le lecteur continuait de renvoyer la première : reconversion à chaque ouverture, accumulation de `.bak.N` sans borne. Corrigé : `JsonNode.Parse` avec `JsonNodeOptions.PropertyNameCaseInsensitive = true` côté dépôt, et le convertisseur écrit désormais via `ArtifactFormatVersionReader.FieldName` plutôt que le littéral `"FormatVersion"`. Testé par `ALowercaseFormatVersionKeyStillConvertsAndDoesNotReconvertOnASecondOpen`.
- **Ruling 39 (Important) — le chemin de sauvegarde était calculé puis jeté.** `ArtifactBackupWriter.CreateBackup` retourne le chemin réellement écrit (C7 : la sauvegarde est le seul chemin de retour); `ProjectWorkspaceRepository.cs` l'ignorait. Corrigé : le chemin est capturé et nommé dans le diagnostic `project.open-failed` quand la conversion ou l'écriture échoue.

Suite : 972/972 sans skip (+4 sur 968 : trois tests bout-en-bout nouveaux, plus un net sur le convertisseur — un test de refus remplacé par deux tests de repli/blanc). `verify-docs` : `Errors: 0`, 121 avertissements, inchangé.

**Preuve empirique que le correctif du Ruling 34 fait bien la différence, pas seulement que le test passe.** L'appel à la récupération a été commenté temporairement dans `ProjectWorkspaceRepository.OpenAsync`, puis `dotnet test --filter FullyQualifiedName~ProjectConversionEndToEndTests` relancé :

```
Échoué APendingSaveTransactionDoesNotUndoAConsentedConversion [57 ms]
Message d'erreur :
 Échec de Assert.AreEqual. Attendu : <1>, Réel : <0>. the pending transaction's stale backup must not win
 over the conversion the operator just consented to: the file must end at the current generation, not be
 silently rolled back to zero.
Échoué!  - échec :     1, réussite :     2, ignorée(s) :     0, total :     3
```

Les deux autres tests du même fichier sont restés verts pendant ce contrôle, ce qui isole l'échec au comportement que le Ruling 34 corrige et à rien d'autre. L'appel a ensuite été restauré et la suite ciblée revérifiée verte (3/3). Le même contrôle a été rejoué au tour 2 (§9) après le déplacement de l'appel à l'intérieur de la branche de conversion, avec le même résultat : échoue sans l'appel, réussit avec.

## 9. Corrections de revue de branche, tour 2 (rulings 40-43)

Une seconde revue, vérifiant les corrections du tour 1 (§8) plutôt que de simplement les accepter, a trouvé que deux d'entre elles avaient régressé quelque chose — l'une un contrat (C2), l'autre un document (§5.4/§5.2 de la spec) que le tour 1 avait lui-même marqué comme à jour sans l'être.

- **Ruling 40 (N1, Critical) — le correctif du Ruling 34 écrivait dans un projet que le binaire refuse.** `AcquireWorkspaceLockAsync` (`ModernProjectStore.cs:769-771`) exécute `Directory.CreateDirectory(".studio")` et ouvre `workspace-save.lock` en `OpenOrCreate` **avant** de vérifier si un répertoire `transactions/` existe seulement (`:723-726`). Appeler la récupération en tête d'`OpenAsync`, comme le faisait le correctif du tour 1, créait donc ces deux entrées dans **tout** projet ouvert — y compris celui qu'on s'apprête à refuser comme trop récent (`project.format-too-new`). C2 : « un binaire qui ne comprend pas ce qu'il lit ne doit jamais pouvoir le réécrire ». `BackwardRefusalTests` ne l'attrapait pas parce qu'il ne comparait que le contenu de `project.json`, jamais le contenu du répertoire. Corrigé : l'appel à `RecoverPendingTransactionsAsync` est déplacé à l'intérieur de la branche `declaredGeneration < ScadaFormatGeneration.Project`, juste avant `PrepareAsync` — la récupération n'est nécessaire que lorsqu'une conversion est sur le point d'être écrite, donc C1 (Ruling 34) reste fermé à l'identique et les chemins de refus ne touchent plus le disque. Revérifié empiriquement à son nouvel emplacement, de la même façon qu'au tour 1 (voir §8, encadré) : le test échoue si l'appel est commenté à son nouvel endroit, et passe restauré. `BackwardRefusalTests.AProjectNewerThanTheBinaryIsRefusedBeforeActivation` porte désormais une assertion positive sur `Directory.GetFileSystemEntries(root)` — le répertoire doit contenir exactement `project.json`, rien de plus — précisément l'assertion dont l'absence avait laissé passer ce défaut.
- **Ruling 41 (N2, préexistant, consigné en écart) — attente de verrou infinie et non annulable sur le chemin de refus.** `AcquireWorkspaceLockAsync:772-789` boucle indéfiniment sur `IOException`, échappable seulement par annulation, et `MainWindow` appelle `OpenAsync` avec le jeton d'annulation par défaut. Avant le correctif du tour 1, le refus revenait en quelques millisecondes sans jamais disputer ce verrou; le Ruling 40 retire l'exposition *nouvelle* que le tour 1 avait introduite sur ce chemin. L'attente infinie **préexiste** pour toute ouverture qui atteint effectivement le store, et la borner touche une infrastructure partagée hors du périmètre de ce chantier : consignée en écart (§6, `KNOWN_GAPS_V2.md` entrée 35) plutôt que corrigée ici.
- **Ruling 42 (Important) — la ligne Status de la spec promue « Implémenté » alors que §5.4 était périmée.** §5.4 spécifiait encore « les trois issues » et une désactivation de sauvegarde en mode consultation via `DisabledReason` — deux choses que C5 avait explicitement retirées et que `ConversionDialogContractTests.TheDialogOffersExactlyTwoOutcomes` interdit activement. Requalifier « Implémenté » une section fausse promeut l'erreur au rang d'autorité. Corrigé : §5.4 réécrite (deux issues, chemin de sauvegarde de base sans mode lecture seule). §5.2 corrigée dans la même passe : elle attribuait l'exécution et l'écriture à `ConversionCoordinator` (qui ne fait que construire le plan et demander le consentement — `PrepareAsync` n'écrit jamais rien) et le chemin de sauvegarde à `ConversionPlan` (que Ruling 13, tour d'origine, avait délibérément laissé ne pas porter). Les deux responsabilités sont maintenant attribuées à `ProjectWorkspaceRepository.OpenAsync` (Infrastructure, §5.3) et au dialogue (§5.4), là où le code les porte réellement.
- **Ruling 43 (Important) — §8 de ce rapport affirmait un travail exact mais se citait lui-même comme preuve dans un fichier que git ignore.** `.superpowers/` est git-ignoré (`git ls-files .superpowers/` ne retourne rien) : quiconque clone ce dépôt recevait une référence vers un fichier qu'il n'a pas. §8 comptait de plus « deux Important » alors que le tour 1 a fermé trois Critical **et** trois Important (six au total, pas quatre). Corrigé : la preuve empirique est rapatriée dans §8 lui-même (encadré ci-dessus), le comptage est exact, et les quatre citations vers `branch-fix-report.md` sont retirées du corps du rapport — ce fichier reste un brouillon de travail local, pas la source de vérité.

Le troisième groupe de corrections du tour 2, non bloquant à lui seul mais inclus dans la même passe :

- **Résidu C3.** Les trois tests bout-en-bout du tour 1 construisent leur fixture via `CreateAsync`, donc chaque page porte déjà un `PageKey` et la branche de règlement de clé du convertisseur n'était jamais que `continue`d — la moitié de C3 qui écrit une identité de page permanente sur disque restait couverte seulement au niveau `JsonNode` (tests unitaires du convertisseur), jamais par le chemin d'ouverture réel. Ajout de `AProjectWithNoPageKeyGetsTheDeterministicKeySettledAndItSurvivesASaveAndReopen` : `PageKey` (et le `HomePageKey` désormais pendant) est retiré du fichier avant rétrogradation à la génération 0; l'ouverture règle la clé à la valeur exacte de `PageKeyFactory.CreateDeterministic(nom, code)`, et cette clé survit à une sauvegarde et une réouverture.
- **BOM UTF-8.** `new StreamWriter(tempStream, Encoding.UTF8)` émet un BOM (`EF BB BF`); `File.WriteAllTextAsync`, remplacé par le tour 1, n'en émettait pas, et aucun autre écrivain JSON du store n'en émet non plus. Remplacé par `new UTF8Encoding(encoderShouldEmitUTF8Identifier: false)`. Testé sur les octets bruts (`File.ReadAllBytesAsync`), puisque `File.ReadAllText[Async]` masque silencieusement un BOM éventuel — les trois tests du tour 1 y étaient donc aveugles par construction.
- **Branche morte.** Dans `ProjectGeneration1Converter`, le bras `Id='…'` du descripteur d'erreur était devenu inatteignable après le correctif du Ruling 37 : `code` n'est blanc que si `PageCode` **et** `Id` le sont tous les deux, donc `identifier` ne pouvait plus jamais être non blanc à cet endroit. Retiré.
- **Nettoyage du fichier temporaire.** Le `catch` qui supprime le fichier temporaire de conversion en cas d'échec pouvait lui-même lever et remplacer l'exception réelle. Isolé dans son propre `try/catch` best-effort qui ne masque jamais l'exception d'origine.

Suite après le tour 2 : 973/973 sans skip (+1 sur 972 : `AProjectWithNoPageKeyGetsTheDeterministicKeySettledAndItSurvivesASaveAndReopen`). `verify-docs` : `Errors: 0`, 121 avertissements, inchangé.

## 10. Corrections de revue de branche, tour 3 (rulings 44-47)

Un troisième tour, vérifiant les corrections du tour 2 (§9) ligne par ligne plutôt qu'en confiance, a trouvé que le déplacement du Ruling 40 n'était pas allé assez loin, qu'une ligne de couverture affirmait comme vraie une propriété non gardée, et qu'un test écrit pour fermer une constatation de revue ne discriminait pas — la septième occurrence de ce motif précis dans ce chantier.

- **Ruling 44 (Critical) — la moitié de N1 survivait, et le Ruling 40 se décrivait lui-même faussement.** Vérifié ligne par ligne : `RecoverPendingTransactionsAsync` (`ProjectWorkspaceRepository.cs:168` au tour 2) précédait `PrepareAsync` (`:170`, qui affiche le dialogue de consentement) **et** la porte `CanProceed` (`:178`). Sur le chemin où l'opérateur **refuse** la conversion, le verrou était donc pris, `.studio/` était créé, et si une transaction était en attente, `RollbackTransaction` remplaçait `project.json` **avant que l'opérateur ait répondu**. Spec §6.5 : « `Annuler` n'ouvre pas le projet : la session précédente reste exactement dans son état, et aucun artefact n'est touché. » Le commentaire de code du tour 2 disait que les chemins de refus « au-dessus » ne touchaient plus le disque — vrai, mais le qualificatif portait tout le sens, et le Ruling 40 et le §9 l'avaient tous deux laissé tomber en généralisant à tort. Corrigé : l'appel est déplacé **sous** la porte `CanProceed`, juste avant le bloc d'écriture (`string? lastBackupPath = null;`). `PrepareAsync` ne lit jamais `project.json` — il résout la chaîne depuis l'entier `declaredGeneration` — donc rien n'est perdu à ce déplacement. Revérifié empiriquement à ce troisième emplacement, comme aux tours 1 et 2 : l'appel commenté fait échouer `APendingSaveTransactionDoesNotUndoAConsentedConversion` avec le même message qu'aux tours précédents (`Attendu : <1>, Réel : <0>`); restauré, vert. Contrôle supplémentaire propre à ce tour : l'ancien emplacement du tour 2 a été réintroduit temporairement en parallèle du nouveau, et `ARefusedConversionNeitherOpensTheProjectNorTouchesIt` a échoué exactement comme prévu (`Attendu : <1>, Réel : <2>` sur le nombre d'entrées du répertoire) avant d'être retiré.
- **Ruling 45 (amélioration structurelle nommée, non exécutée) — remonter la garde `Directory.Exists(transactionsRoot)` au-dessus d'`AcquireWorkspaceLockAsync` dans le store fermerait l'invariant plus proprement.** Proposé en revue, jugé structurellement supérieur, **délibérément non fait ici** : `AcquireWorkspaceLockAsync` est une infrastructure partagée par cinq appelants dans `ModernProjectStore`, et y toucher au tout dernier correctif d'une branche pour restaurer un invariant que la revue elle-même établit comme inatteignable aujourd'hui (`RollbackTransaction` ne se déclenche que dans des phases où la sauvegarde est une copie du fichier que la pré-lecture vient de lire) est un risque disproportionné à son bénéfice immédiat. Consigné comme amélioration nommée pour un futur chantier touchant `ModernProjectStore`, pas comme dette silencieuse.
- **Ruling 46 (Important) — la ligne de couverture ajoutée au tour 2 documentait comme couverte la propriété exacte que le Ruling 44 vient de fermer, alors qu'elle ne l'était pas encore.** `REGRESSION_COVERAGE_V2.md` créditait `BackwardRefusalTests` du « declined-conversion no-touch », propriété qu'`ARefusedConversionNeitherOpensTheProjectNorTouchesIt` n'assertait pas : le test ne vérifiait que le contenu de `project.json` et l'absence de `.bak`, jamais le contenu du répertoire — exactement ce qui avait laissé passer le Ruling 44. Corrigé : la même assertion `Directory.GetFileSystemEntries` que porte déjà le test du refus trop-récent est ajoutée à ce test. La ligne de couverture est maintenant vraie.
- **Ruling 47 (Important) — le test du reste de C3, ajouté au tour 2 pour fermer une constatation de revue, ne discriminait pas.** Septième occurrence de ce motif dans ce chantier. Si le convertisseur n'écrivait rien du tout, `PageKey` se désérialiserait en `Guid.Empty`, et `ModernProjectMigration.NormalizeIdentity` — exercé à chaque chargement (Ruling 16) — dériverait en mémoire la **même** clé `CreateDeterministic(nom, code)` avant que l'instantané ne soit retourné. Les deux assertions du tour 2 (`page.PageKey == expectedKey`, `page.PageKey != Guid.Empty`) passeraient dans les deux cas et ne séparaient donc pas le convertisseur du normaliseur qu'il existe précisément pour rendre superflu — alors que « régler l'identité *dans le fichier* » est la justification entière du convertisseur. Corrigé : après la première ouverture, `project.json` est relu directement depuis le disque et la clé qu'il porte est comparée, en quatre lignes, à `expectedKey.ToString("D")`, avant même la sauvegarde/réouverture qui suit.

Suite après le tour 3 : 973/973 sans skip (aucun nouveau test — les deux corrections de ce tour renforcent des assertions dans des tests existants). `verify-docs` : `Errors: 0`, 121 avertissements, inchangé.
