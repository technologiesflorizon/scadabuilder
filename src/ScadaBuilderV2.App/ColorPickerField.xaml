<UserControl x:Class="ScadaBuilderV2.App.ColorPickerField"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinHeight="32">
    <Button Padding="0"
            Margin="0,3,0,8"
            HorizontalContentAlignment="Stretch"
            Click="OnOpenPickerClick">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="42"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0"
                    Height="22"
                    Margin="6,4"
                    BorderBrush="#33000000"
                    BorderThickness="1"
                    CornerRadius="3">
                <Grid>
                    <Grid x:Name="TransparencyPattern">
                        <Grid.Background>
                            <DrawingBrush TileMode="Tile"
                                          Viewport="0,0,8,8"
                                          ViewportUnits="Absolute">
                                <DrawingBrush.Drawing>
                                    <DrawingGroup>
                                        <GeometryDrawing Brush="#FFFFFFFF">
                                            <GeometryDrawing.Geometry>
                                                <RectangleGeometry Rect="0,0,8,8"/>
                                            </GeometryDrawing.Geometry>
                                        </GeometryDrawing>
                                        <GeometryDrawing Brush="#FFE4E8EA">
                                            <GeometryDrawing.Geometry>
                                                <GeometryGroup>
                                                    <RectangleGeometry Rect="0,0,4,4"/>
                                                    <RectangleGeometry Rect="4,4,4,4"/>
                                                </GeometryGroup>
                                            </GeometryDrawing.Geometry>
                                        </GeometryDrawing>
                                    </DrawingGroup>
                                </DrawingBrush.Drawing>
                            </DrawingBrush>
                        </Grid.Background>
                    </Grid>
                    <Border x:Name="ColorPreview" CornerRadius="2"/>
                </Grid>
            </Border>
            <TextBlock x:Name="ColorText"
                       Grid.Column="1"
                       VerticalAlignment="Center"
                       Margin="0,0,8,0"
                       TextTrimming="CharacterEllipsis"/>
        </Grid>
    </Button>
</UserControl>
