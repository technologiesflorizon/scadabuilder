<Window x:Class="ScadaBuilderV2.App.ExpressionCreationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Assistant d'expression"
        Width="620" Height="470"
        MinWidth="560" MinHeight="400"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style x:Key="OperatorButtonStyle" TargetType="Button">
            <Setter Property="MinWidth" Value="42"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="Margin" Value="3"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2090A0"/>
            <Setter Property="BorderBrush" Value="#0F7280"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="14,7"/>
        </Style>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Composez votre expression :" FontWeight="SemiBold" Margin="0,0,0,8"/>
        <WrapPanel Grid.Row="1" Margin="-3,0,-3,8">
            <Button Content="Variable" ToolTip="Insérer un tag" Click="OnVariableClick" Margin="3" Padding="10,4"/>
            <Button Content=">" Tag=" &gt; " ToolTip="Supérieur à ( &gt; )" Style="{StaticResource OperatorButtonStyle}" Click="OnOperatorClick"/>
            <Button Content="&lt;" Tag=" &lt; " ToolTip="Inférieur à ( &lt; )" Style="{StaticResource OperatorButtonStyle}" Click="OnOperatorClick"/>
            <Button Content="&gt;=" Tag=" &gt;= " ToolTip="Supérieur ou égal à ( &gt;= )" Style="{StaticResource OperatorButtonStyle}" Click="OnOperatorClick"/>
            <Button Content="&lt;=" Tag=" &lt;= " ToolTip="Inférieur ou égal à ( &lt;= )" Style="{StaticResource OperatorButtonStyle}" Click="OnOperatorClick"/>
            <Button Content="==" Tag=" == " ToolTip="Égal à ( == )" Style="{StaticResource OperatorButtonStyle}" Click="OnOperatorClick"/>
            <Button Content="!=" Tag=" != " ToolTip="Différent de ( != )" Style="{StaticResource OperatorButtonStyle}" Click="OnOperatorClick"/>
            <Button Content="ET (&amp;&amp;)" Tag=" &amp;&amp; " ToolTip="Et logique ( &amp;&amp; )" Style="{StaticResource OperatorButtonStyle}" Click="OnOperatorClick"/>
            <Button Content="OU (||)" Tag=" || " ToolTip="Ou logique ( || )" Style="{StaticResource OperatorButtonStyle}" Click="OnOperatorClick"/>
            <Button Content="( )" ToolTip="Insérer une paire de parenthèses" Style="{StaticResource OperatorButtonStyle}" Click="OnParenthesesClick"/>
            <Button Content="Effacer" ToolTip="Effacer l'expression" Click="OnClearClick" Margin="3" Padding="10,4"/>
        </WrapPanel>
        <TextBox x:Name="ExpressionTextBox" Grid.Row="2"
                 AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" MinHeight="120"
                 TextChanged="OnExpressionTextChanged"/>
        <TextBlock x:Name="ValidationText" Grid.Row="3" TextWrapping="Wrap" Margin="0,8,0,0"/>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Annuler" IsCancel="True" MinWidth="90" Margin="0,0,8,0"/>
            <Button Content="Appliquer" IsDefault="True" MinWidth="100" Style="{StaticResource PrimaryButtonStyle}" Click="OnApplyClick"/>
        </StackPanel>
    </Grid>
</Window>
