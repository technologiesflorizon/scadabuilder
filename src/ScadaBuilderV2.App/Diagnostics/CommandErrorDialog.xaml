<Window x:Class="ScadaBuilderV2.App.Diagnostics.CommandErrorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Erreur de commande"
        Width="620" Height="390" MinWidth="520" MinHeight="320"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Background="#F5F7FA">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition/></Grid.ColumnDefinitions>
            <Border Width="42" Height="42" CornerRadius="21" Background="#FDECEC" Margin="0,0,14,0">
                <TextBlock Text="!" Foreground="#B42318" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <StackPanel Grid.Column="1">
                <TextBlock Text="La commande n'a pas pu être exécutée" FontSize="18" FontWeight="SemiBold" Foreground="#1D2939"/>
                <TextBlock Text="{Binding Summary}" Margin="0,5,0,0" TextWrapping="Wrap" Foreground="#475467"/>
            </StackPanel>
        </Grid>
        <WrapPanel Grid.Row="1" Margin="56,16,0,12">
            <Border Background="#FEE4E2" CornerRadius="4" Padding="9,4" Margin="0,0,8,0">
                <TextBlock Foreground="#B42318"><Run Text="{Binding ErrorCount}"/><Run Text=" erreur(s)"/></TextBlock>
            </Border>
            <Border Background="#FEF0C7" CornerRadius="4" Padding="9,4">
                <TextBlock Foreground="#93370D"><Run Text="{Binding WarningCount}"/><Run Text=" avertissement(s)"/></TextBlock>
            </Border>
        </WrapPanel>
        <Border Grid.Row="2" Background="White" BorderBrush="#D0D5DD" BorderThickness="1" CornerRadius="6" Padding="12">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Causes}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,4">
                                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition/></Grid.ColumnDefinitions>
                                <TextBlock Text="•" Foreground="#B42318" Margin="0,0,8,0"/>
                                <TextBlock Grid.Column="1" Text="{Binding}" TextWrapping="Wrap" Foreground="#344054"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Afficher les erreurs" MinWidth="150" Height="34" Margin="0,0,8,0" Click="OnShowErrorsClick"/>
            <Button Content="Fermer" MinWidth="90" Height="34" IsDefault="True" Click="OnCloseClick"/>
        </StackPanel>
    </Grid>
</Window>
